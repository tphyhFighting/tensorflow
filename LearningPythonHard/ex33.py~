#!/usr/bin/env python
# -*- coding:utf-8 -*-
#while i < 6:
    #print "At the top i is %d"%i
    #numbers.append(i)
    #i = i+1
    #print "Numbers now:",numbers
    #print "At the bottom i is %d"%i
numbers = []
def ins_number(mNumber, step):
    #i = 0
    #while i < mNumber:
    for i in range(mNumber):
        print "At the top i is %d"%i
        numbers.append(i)
        i += step
        print "Numbers now:",numbers
        print "At the bottom i is %d"%i
ins_number(6,2)  
print "The numbers:"
for num in numbers:
    print num

