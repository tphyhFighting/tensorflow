模块包的一些笔记:
包导入路径中使用点号
python3.0的绝对导入并不能保证找到标准库中的模块
python3.0中可以用如下语句进行相对导入：
	from . import 
	from .. import 
sys.path模块搜索路径包含了:程序的包含目录，PYTHONPATH设置，路径文件设置以及标准库。
import或from语句中所列出的每个目录都必须含有__init__.py文件。其他目录则不需要包含这个文件，包括含有包路径最左侧组件的目录。

